name: Update UFC Dataset

on:
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch:

jobs:
  update-and-upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.13"

      - name: Cache HTML files
        uses: actions/cache@v3
        with:
          path: cached_html
          key: cached-html-${{ github.run_number }}

      - name: Configure Kaggle credentials
        run: |
          mkdir -p ~/.kaggle
          echo "{\"username\":\"${{ secrets.KAGGLE_USERNAME }}\",\"key\":\"${{ secrets.KAGGLE_KEY }}\"}" > ~/.kaggle/kaggle.json
          chmod 600 ~/.kaggle/kaggle.json

      - name: Install dependencies + Kaggle CLI
        run: |
          pip install -r requirements.txt
          pip install kaggle

      - name: Run pipeline
        run: python main.py

      - name: Create zip files
        run: |
          mkdir -p upload
          TODAY=$(date '+%Y-%m-%d')
            zip -jr "upload_ready/data.zip" data/
            zip -jr "upload_ready/raw_data.zip" raw_data/

      - name: Upload new version
        run: |
          kaggle datasets version -p upload_ready -m "Automated monthly update — $(date '+%Y-%m-%d')"
